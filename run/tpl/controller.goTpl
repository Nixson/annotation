package controller

import (
	"github.com/Nixson/http-server/server"
	ctr "${path}"
)

func InitController${name}() {
	var ctx server.ContextInterface
	ctx = &ctr.${name}{}
	server.InitController("${name}", &ctx)
}
