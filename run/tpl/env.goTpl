package genX

import (
	"embed"
	"github.com/Nixson/environment"
	"github.com/Nixson/logNx"
	${controllerPath}${listenerPath})

func InitMain(embedMain embed.FS) {
	environment.InitEnv(embedMain)
	if logNx.IfDebug() {
		logNx.Get().Debug("init ENV\t done")
	}
	${controllerInit}${listenerInit}
}
